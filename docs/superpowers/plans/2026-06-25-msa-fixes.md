# MSA 구조 개선 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 검토에서 발견된 6개 MSA 구조 문제(단일 DB, WebFlux+JPA 혼용, Rate Limiting 미동작, Discovery Locator 이중 노출, Gateway 에러 형식 불일치, Flyway 마이그레이션 파일 없음)를 모두 수정한다.

**Architecture:** PostgreSQL 스키마 분리로 서비스별 DB 네임스페이스를 격리하고, notification을 WebFlux+R2DBC로 전환한다. Gateway에 Redis Rate Limiting과 공통 에러 핸들러를 추가하고 Discovery Locator를 비활성화한다.

**Tech Stack:** Spring Boot 4.0.3, Spring Cloud 2025.0.0, Spring Data R2DBC, r2dbc-postgresql, Spring Cloud Gateway (WebFlux), Redis, Flyway, PostgreSQL

## Global Constraints

- 기술 스택 버전 다운그레이드 절대 금지
- V*.sql 마이그레이션 파일은 추가만 가능, 수정 금지
- main/dev/develop 브랜치에서 직접 커밋 금지 — `feat/msa-fixes` 브랜치 사용
- 커밋 제목 영어, 본문 한국어, Co-Authored-By 표시 금지
- 각 서비스는 자신의 모듈만 수정 (common, gateway는 팀 합의 영역이므로 이 플랜에서 일괄 처리)
- Google Java Style, 들여쓰기 스페이스 4칸
- else 예약어 사용 금지, 메서드 18줄 이내, setter 금지

---

## 파일 맵

### Task 1 — feat 브랜치 생성
없음

### Task 2 — PostgreSQL 스키마 분리
- Modify: `member/src/main/resources/application.yml`
- Modify: `payment/src/main/resources/application.yml`
- Modify: `search/src/main/resources/application.yml`
- Modify: `notification/src/main/resources/application.yml`

### Task 3 — notification WebFlux + R2DBC 전환
- Modify: `notification/build.gradle`
- Modify: `notification/src/main/resources/application.yml`
- Create: `notification/src/main/resources/db/migration/V1__init_schema.sql`

### Task 4 — Gateway Redis 설정 키 수정 + Rate Limiting 구현
- Modify: `gateway/build.gradle`
- Modify: `gateway/src/main/resources/application.yml`
- Create: `gateway/src/main/java/com/haagendazs/gateway/config/RateLimiterConfig.java`

### Task 5 — Discovery Locator 비활성화
- Modify: `gateway/src/main/resources/application.yml` (Task 4와 동일 파일, 순서 주의)

### Task 6 — Gateway 공통 에러 핸들러 (common 의존성 추가)
- Modify: `gateway/build.gradle` (Task 4와 동일 파일)
- Create: `gateway/src/main/java/com/haagendazs/gateway/exception/GlobalGatewayExceptionHandler.java`

### Task 7 — 나머지 서비스 Flyway 초기 마이그레이션 파일 생성
- Create: `member/src/main/resources/db/migration/V1__init_schema.sql`
- Create: `payment/src/main/resources/db/migration/V1__init_schema.sql`
- Create: `search/src/main/resources/db/migration/V1__init_schema.sql`

### Task 8 — build.gradle 각 모듈 주석 정리
- Modify: `member/build.gradle`
- Modify: `payment/build.gradle`
- Modify: `search/build.gradle`
- Modify: `notification/build.gradle`
- Modify: `gateway/build.gradle`
- Modify: `common/build.gradle`

---

## Task 1: feat 브랜치 생성

**Files:** 없음

- [ ] **Step 1: 브랜치 생성**

```bash
git checkout -b feat/msa-fixes
```

Expected: `Switched to a new branch 'feat/msa-fixes'`

---

## Task 2: PostgreSQL 스키마 분리

**배경:** 4개 서비스가 동일한 `haagendazs` DB를 공유하면 Flyway가 `flyway_schema_history` 테이블 충돌을 일으킨다. PostgreSQL 스키마(논리적 네임스페이스)를 서비스별로 분리해 격리한다.

`spring.flyway.schemas` — Flyway가 관리할 스키마 목록 (없으면 자동 생성)
`spring.flyway.table` — 서비스별 히스토리 테이블명 분리
`spring.datasource.url`에 `currentSchema=` 파라미터 추가 — JPA가 해당 스키마에서 테이블을 찾도록

**Files:**
- Modify: `member/src/main/resources/application.yml`
- Modify: `payment/src/main/resources/application.yml`
- Modify: `search/src/main/resources/application.yml`
- Modify: `notification/src/main/resources/application.yml`

- [ ] **Step 1: member application.yml 수정**

`member/src/main/resources/application.yml`의 `datasource`와 `flyway` 섹션을 아래와 같이 교체:

```yaml
server:
  port: 8084

spring:
  application:
    name: member
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}
  datasource:
    # currentSchema 파라미터로 JPA 기본 스키마를 member로 고정
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:haagendazs}?currentSchema=member
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration
    # 서비스 전용 스키마에서 히스토리 테이블 관리 (타 서비스와 충돌 방지)
    schemas: member
    table: flyway_schema_history

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: member

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
```

- [ ] **Step 2: payment application.yml 수정**

`payment/src/main/resources/application.yml`:

```yaml
server:
  port: 8083

spring:
  application:
    name: payment
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:haagendazs}?currentSchema=payment
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration
    schemas: payment
    table: flyway_schema_history

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: payment

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
```

- [ ] **Step 3: search application.yml 수정**

`search/src/main/resources/application.yml`:

```yaml
server:
  port: 8082

spring:
  application:
    name: search
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:haagendazs}?currentSchema=search
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration
    schemas: search
    table: flyway_schema_history
  elasticsearch:
    uris: http://${ES_HOST:localhost}:${ES_PORT:9200}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: search

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
```

- [ ] **Step 4: notification application.yml 수정 (스키마 분리만, R2DBC 전환은 Task 3)**

`notification/src/main/resources/application.yml` — R2DBC 전환 전 임시로 스키마 분리만 반영:

> **주의:** Task 3에서 이 파일을 R2DBC 방식으로 완전 교체하므로, 이 스텝은 Task 3 Step 1에 통합됨. 스킵.

- [ ] **Step 5: 커밋**

```bash
git add member/src/main/resources/application.yml \
        payment/src/main/resources/application.yml \
        search/src/main/resources/application.yml
git commit -m "feat: separate PostgreSQL schema per service

- member/payment/search 각 서비스가 독립 PostgreSQL 스키마 사용
- currentSchema JDBC 파라미터로 JPA 기본 스키마 고정
- flyway.schemas + flyway.table로 히스토리 테이블 격리
- flyway_schema_history 충돌 방지"
```

---

## Task 3: notification — WebFlux + R2DBC 전환

**배경:** notification은 Netty(WebFlux) 위에서 동작해야 하므로 블로킹 JPA 대신 R2DBC를 사용한다. Flyway는 R2DBC와 공존 가능하며 스키마 생성에 계속 사용한다.

**Files:**
- Modify: `notification/build.gradle`
- Modify: `notification/src/main/resources/application.yml`
- Create: `notification/src/main/resources/db/migration/V1__init_schema.sql`

- [ ] **Step 1: notification/build.gradle 교체**

```groovy
plugins {
    id 'org.springframework.boot'
}

// notification 서비스 — WebFlux(Netty) + R2DBC(논블로킹 PostgreSQL)
// JPA/Hibernate 사용 금지: Netty 이벤트 루프에서 블로킹 JDBC 호출은 스레드 고갈을 유발함
// 엔티티: @Table (Spring Data R2DBC) 사용, @Entity (JPA) 사용 금지
// Repository: ReactiveCrudRepository 또는 R2dbcRepository 상속
// 트랜잭션: @Transactional (org.springframework.transaction) 사용 가능 — R2DBC TransactionManager가 처리
dependencies {
    implementation project(':common')

    // WebFlux (Netty 런타임)
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation 'org.springframework.cloud:spring-cloud-starter-config'

    // R2DBC (논블로킹 PostgreSQL 드라이버)
    implementation 'org.springframework.boot:spring-boot-starter-data-r2dbc'
    implementation 'org.postgresql:r2dbc-postgresql'

    // Redis (Reactive)
    implementation 'org.springframework.boot:spring-boot-starter-data-redis-reactive'

    // Flyway — R2DBC와 공존 가능, 스키마 마이그레이션 전용 (JDBC URL 별도 설정 필요)
    implementation 'org.flywaydb:flyway-core'
    // Flyway는 내부적으로 JDBC를 사용하므로 JDBC 드라이버를 런타임에 추가
    runtimeOnly 'org.postgresql:postgresql'

    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
}
```

- [ ] **Step 2: notification application.yml 교체**

```yaml
server:
  port: 8081

spring:
  application:
    name: notification
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}

  # R2DBC — 논블로킹 DB 연결 (Netty 이벤트 루프 전용)
  r2dbc:
    url: r2dbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:haagendazs}?schema=notification
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  # Flyway는 JDBC URL로 별도 동작 (R2DBC URL과 독립적)
  flyway:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:haagendazs}
    user: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    locations: classpath:db/migration
    schemas: notification
    table: flyway_schema_history

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: notification

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
```

- [ ] **Step 3: V1__init_schema.sql 생성**

`notification/src/main/resources/db/migration/V1__init_schema.sql`:

```sql
-- notification 서비스 전용 스키마 초기화
-- 이 파일은 수정 금지 (Flyway 불변 원칙)
CREATE SCHEMA IF NOT EXISTS notification;
```

- [ ] **Step 4: 커밋**

```bash
git add notification/build.gradle \
        notification/src/main/resources/application.yml \
        notification/src/main/resources/db/migration/V1__init_schema.sql
git commit -m "feat: migrate notification to WebFlux + R2DBC

- spring-boot-starter-data-jpa 제거, spring-boot-starter-data-r2dbc 추가
- r2dbc-postgresql 드라이버 추가
- Flyway는 JDBC URL 분리 설정으로 R2DBC와 공존
- notification 스키마 격리 (V1__init_schema.sql)
- Netty 이벤트 루프 블로킹 호출 제거"
```

---

## Task 4 + 5 + 6: Gateway 개선 (Redis 키 수정 + Rate Limiting + Discovery Locator 비활성화 + 공통 에러 핸들러)

> Task 4/5/6은 모두 gateway 모듈을 수정하므로 하나의 태스크로 처리한다.

**Files:**
- Modify: `gateway/build.gradle`
- Modify: `gateway/src/main/resources/application.yml`
- Create: `gateway/src/main/java/com/haagendazs/gateway/config/RateLimiterConfig.java`
- Create: `gateway/src/main/java/com/haagendazs/gateway/exception/GlobalGatewayExceptionHandler.java`

- [ ] **Step 1: gateway/build.gradle 교체**

```groovy
plugins {
    id 'org.springframework.boot'
}

// gateway — Spring Cloud Gateway (WebFlux/Netty 기반 API 게이트웨이)
// common 모듈 의존: GlobalExceptionHandler AutoConfiguration 적용 불가 (WebFlux 환경)
//   → gateway 전용 GlobalGatewayExceptionHandler를 별도 구현 (ErrorWebExceptionHandler)
// Rate Limiting: Redis 기반 RequestRateLimiter 필터 사용
//   → RateLimiterConfig에서 KeyResolver 빈 등록 필수
// CircuitBreaker: Resilience4j 리액티브 모듈 사용
dependencies {
    implementation project(':common')

    implementation 'org.springframework.cloud:spring-cloud-starter-gateway'
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation 'org.springframework.cloud:spring-cloud-starter-config'
    implementation 'org.springframework.boot:spring-boot-starter-data-redis-reactive'
    implementation 'org.springframework.cloud:spring-cloud-starter-circuitbreaker-reactor-resilience4j'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
}
```

- [ ] **Step 2: gateway application.yml 전체 교체**

```yaml
server:
  port: 8080

spring:
  application:
    name: gateway
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}

  # Redis Rate Limiting용 — spring.data.redis가 올바른 키
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

  cloud:
    gateway:
      discovery:
        locator:
          # false: 명시적 routes만 유효. true이면 /serviceId/** 경로가 자동 생성되어
          # 모든 서비스 내부 API가 게이트웨이 필터 없이 이중 노출됨
          enabled: false
      routes:
        - id: notification-service
          uri: lb://notification
          predicates:
            - Path=/api/notifications/**
          filters:
            - name: RequestRateLimiter
              args:
                # ipKeyResolver 빈은 RateLimiterConfig에서 등록
                key-resolver: "#{@ipKeyResolver}"
                redis-rate-limiter.replenishRate: 20
                redis-rate-limiter.burstCapacity: 40
                redis-rate-limiter.requestedTokens: 1

        - id: search-service
          uri: lb://search
          predicates:
            - Path=/api/search/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@ipKeyResolver}"
                redis-rate-limiter.replenishRate: 30
                redis-rate-limiter.burstCapacity: 60
                redis-rate-limiter.requestedTokens: 1

        - id: payment-service
          uri: lb://payment
          predicates:
            - Path=/api/payments/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@ipKeyResolver}"
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                redis-rate-limiter.requestedTokens: 1

        - id: member-service
          uri: lb://member
          predicates:
            - Path=/api/members/**
          filters:
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@ipKeyResolver}"
                redis-rate-limiter.replenishRate: 20
                redis-rate-limiter.burstCapacity: 40
                redis-rate-limiter.requestedTokens: 1

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: gateway

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URI:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
```

- [ ] **Step 3: RateLimiterConfig.java 생성**

`gateway/src/main/java/com/haagendazs/gateway/config/RateLimiterConfig.java`:

```java
package com.haagendazs.gateway.config;

import org.springframework.cloud.gateway.filter.ratelimit.KeyResolver;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import reactor.core.publisher.Mono;

@Configuration
public class RateLimiterConfig {

    @Bean
    public KeyResolver ipKeyResolver() {
        return exchange -> Mono.justOrEmpty(
                exchange.getRequest().getRemoteAddress()
        ).map(address -> address.getAddress().getHostAddress());
    }
}
```

- [ ] **Step 4: GlobalGatewayExceptionHandler.java 생성**

`gateway/src/main/java/com/haagendazs/gateway/exception/GlobalGatewayExceptionHandler.java`:

```java
package com.haagendazs.gateway.exception;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.haagendazs.common.exception.BusinessException;
import com.haagendazs.common.exception.ErrorCode;
import com.haagendazs.common.response.ApiResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.boot.web.reactive.error.ErrorWebExceptionHandler;
import org.springframework.core.annotation.Order;
import org.springframework.core.io.buffer.DataBuffer;
import org.springframework.http.HttpStatus;
import org.springframework.http.MediaType;
import org.springframework.stereotype.Component;
import org.springframework.web.server.ResponseStatusException;
import org.springframework.web.server.ServerWebExchange;
import reactor.core.publisher.Mono;

@Order(-1)
@Component
@RequiredArgsConstructor
public class GlobalGatewayExceptionHandler implements ErrorWebExceptionHandler {

    private final ObjectMapper objectMapper;

    @Override
    public Mono<Void> handle(ServerWebExchange exchange, Throwable ex) {
        ApiResponse<Void> body = resolveBody(ex);
        HttpStatus status = resolveStatus(ex);

        exchange.getResponse().setStatusCode(status);
        exchange.getResponse().getHeaders().setContentType(MediaType.APPLICATION_JSON);

        return writeResponse(exchange, body);
    }

    private ApiResponse<Void> resolveBody(Throwable ex) {
        if (ex instanceof BusinessException businessException) {
            return ApiResponse.fail(businessException.getErrorCode());
        }
        if (ex instanceof ResponseStatusException responseStatusException) {
            return ApiResponse.fail(ErrorCode.INTERNAL_SERVER_ERROR,
                    responseStatusException.getReason());
        }
        return ApiResponse.fail(ErrorCode.INTERNAL_SERVER_ERROR);
    }

    private HttpStatus resolveStatus(Throwable ex) {
        if (ex instanceof BusinessException businessException) {
            return businessException.getErrorCode().getHttpStatus();
        }
        if (ex instanceof ResponseStatusException responseStatusException) {
            return HttpStatus.valueOf(responseStatusException.getStatusCode().value());
        }
        return HttpStatus.INTERNAL_SERVER_ERROR;
    }

    private Mono<Void> writeResponse(ServerWebExchange exchange, ApiResponse<Void> body) {
        try {
            byte[] bytes = objectMapper.writeValueAsBytes(body);
            DataBuffer buffer = exchange.getResponse().bufferFactory().wrap(bytes);
            return exchange.getResponse().writeWith(Mono.just(buffer));
        } catch (JsonProcessingException e) {
            return exchange.getResponse().setComplete();
        }
    }
}
```

- [ ] **Step 5: 커밋**

```bash
git add gateway/build.gradle \
        gateway/src/main/resources/application.yml \
        gateway/src/main/java/com/haagendazs/gateway/config/RateLimiterConfig.java \
        gateway/src/main/java/com/haagendazs/gateway/exception/GlobalGatewayExceptionHandler.java
git commit -m "feat: fix gateway rate limiting, discovery locator, error handler

- spring.cloud.redis → spring.data.redis (올바른 Spring Boot 키)
- RequestRateLimiter 필터 + ipKeyResolver 빈 추가 (IP 기반 Rate Limiting)
- discovery.locator.enabled: false (이중 경로 노출 차단)
- common 모듈 의존성 추가
- GlobalGatewayExceptionHandler로 WebFlux 에러 응답 형식 통일"
```

---

## Task 7: 나머지 서비스 Flyway 초기 마이그레이션 파일 생성

**배경:** `ddl-auto: validate`는 엔티티 클래스가 추가되는 순간 마이그레이션 파일 없이 기동 실패한다. 각 서비스 스키마를 미리 생성해두는 V1 파일을 추가한다.

**Files:**
- Create: `member/src/main/resources/db/migration/V1__init_schema.sql`
- Create: `payment/src/main/resources/db/migration/V1__init_schema.sql`
- Create: `search/src/main/resources/db/migration/V1__init_schema.sql`

- [ ] **Step 1: member V1 마이그레이션 생성**

`member/src/main/resources/db/migration/V1__init_schema.sql`:

```sql
-- member 서비스 전용 스키마 초기화
-- 이 파일은 수정 금지 (Flyway 불변 원칙)
CREATE SCHEMA IF NOT EXISTS member;
```

- [ ] **Step 2: payment V1 마이그레이션 생성**

`payment/src/main/resources/db/migration/V1__init_schema.sql`:

```sql
-- payment 서비스 전용 스키마 초기화
-- 이 파일은 수정 금지 (Flyway 불변 원칙)
CREATE SCHEMA IF NOT EXISTS payment;
```

- [ ] **Step 3: search V1 마이그레이션 생성**

`search/src/main/resources/db/migration/V1__init_schema.sql`:

```sql
-- search 서비스 전용 스키마 초기화
-- 이 파일은 수정 금지 (Flyway 불변 원칙)
CREATE SCHEMA IF NOT EXISTS search;
```

- [ ] **Step 4: 커밋**

```bash
git add member/src/main/resources/db/migration/V1__init_schema.sql \
        payment/src/main/resources/db/migration/V1__init_schema.sql \
        search/src/main/resources/db/migration/V1__init_schema.sql
git commit -m "feat: add V1 Flyway migration for schema initialization

- member/payment/search 각 서비스 전용 PostgreSQL 스키마 생성
- ddl-auto: validate 환경에서 엔티티 추가 시 기동 실패 방지"
```

---

## Task 8: build.gradle 모듈별 사용법 주석 추가

**Files:**
- Modify: `member/build.gradle`
- Modify: `payment/build.gradle`
- Modify: `search/build.gradle`
- Modify: `notification/build.gradle` (Task 3에서 이미 처리됨 — 스킵)
- Modify: `gateway/build.gradle` (Task 4에서 이미 처리됨 — 스킵)
- Modify: `common/build.gradle`

- [ ] **Step 1: member/build.gradle 주석 추가**

```groovy
plugins {
    id 'org.springframework.boot'
}

// member 서비스 — MVC(Tomcat) 기반 회원 도메인
// DB: PostgreSQL, 스키마: member (application.yml currentSchema=member)
// ORM: Spring Data JPA + Hibernate (블로킹 JDBC — Tomcat 스레드 모델에서만 사용할 것)
// 마이그레이션: Flyway V{n}__{설명}.sql — 추가만 가능, 수정 금지
// QueryDSL 사용 시 루트 build.gradle의 querydslVersion 참조하여 의존성 추가
dependencies {
    implementation project(':common')

    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation 'org.springframework.cloud:spring-cloud-starter-config'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
    runtimeOnly 'org.postgresql:postgresql'
}
```

- [ ] **Step 2: payment/build.gradle 주석 추가**

```groovy
plugins {
    id 'org.springframework.boot'
}

// payment 서비스 — MVC(Tomcat) 기반 결제 도메인
// DB: PostgreSQL, 스키마: payment (application.yml currentSchema=payment)
// ORM: Spring Data JPA + Hibernate (블로킹 JDBC — Tomcat 스레드 모델에서만 사용할 것)
// 마이그레이션: Flyway V{n}__{설명}.sql — 추가만 가능, 수정 금지
// 동시성: 결제는 핵심 도메인 — Optimistic/Pessimistic Lock 전략 신중히 선택할 것
// QueryDSL 사용 시 루트 build.gradle의 querydslVersion 참조하여 의존성 추가
dependencies {
    implementation project(':common')

    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation 'org.springframework.cloud:spring-cloud-starter-config'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
    runtimeOnly 'org.postgresql:postgresql'
}
```

- [ ] **Step 3: search/build.gradle 주석 추가**

```groovy
plugins {
    id 'org.springframework.boot'
}

// search 서비스 — MVC(Tomcat) 기반 검색 도메인
// DB: PostgreSQL (스키마: search) + Elasticsearch 이중 사용
// ORM: Spring Data JPA (PostgreSQL) + Spring Data Elasticsearch
// 마이그레이션: Flyway V{n}__{설명}.sql — 추가만 가능, 수정 금지
// Elasticsearch 인덱스 관리는 Flyway 대상 아님 — 별도 초기화 로직 구현
dependencies {
    implementation project(':common')

    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation 'org.springframework.cloud:spring-cloud-starter-config'
    implementation 'org.springframework.boot:spring-boot-starter-data-elasticsearch'
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.flywaydb:flyway-core'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
    runtimeOnly 'io.micrometer:micrometer-registry-prometheus'
    runtimeOnly 'org.postgresql:postgresql'
}
```

- [ ] **Step 4: common/build.gradle 주석 추가**

```groovy
// common — 라이브러리 모듈 (Spring Boot 앱 아님, bootJar 비활성화됨)
// AutoConfiguration 등록: META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
//   현재 등록된 빈: GlobalExceptionHandler (MVC 전용 @RestControllerAdvice)
//   → gateway(WebFlux)에서는 동작하지 않음 — gateway는 GlobalGatewayExceptionHandler 사용
// web/validation은 compileOnly: 의존하는 서비스가 직접 선언해야 함
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter'
    compileOnly 'org.springframework.boot:spring-boot-starter-validation'
    compileOnly 'org.springframework.boot:spring-boot-starter-web'
}
```

- [ ] **Step 5: 커밋**

```bash
git add member/build.gradle \
        payment/build.gradle \
        search/build.gradle \
        common/build.gradle
git commit -m "docs: add usage comments to each module build.gradle

- 각 모듈의 기술 선택 이유, 주의사항, 확장 방법을 주석으로 문서화
- 팀원이 의존성 추가/수정 시 참고할 수 있도록 안내"
```

---

## 자기 검토 (Self-Review)

### Spec 커버리지
| 이슈 | 처리 태스크 |
|------|------------|
| 단일 DB / Flyway 충돌 | Task 2, Task 7 |
| notification WebFlux+JPA 혼용 | Task 3 |
| Gateway Rate Limiting 미동작 | Task 4+5+6 Step 2,3 |
| Discovery Locator 이중 노출 | Task 4+5+6 Step 2 |
| Gateway 에러 형식 불일치 | Task 4+5+6 Step 1,4 |
| Flyway 마이그레이션 파일 없음 | Task 3 Step 3, Task 7 |
| build.gradle 사용법 주석 | Task 8 |

### 플레이스홀더 없음 — 모든 스텝에 실제 코드 포함 확인

### 타입 일관성
- `ApiResponse.fail(ErrorCode, String)` — `common/exception/ErrorCode.java`에 정의된 시그니처와 일치
- `BusinessException.getErrorCode()` — `common/exception/BusinessException.java`에 정의된 메서드와 일치
- `ipKeyResolver` 빈 이름 — `RateLimiterConfig`의 `@Bean` 메서드명과 `application.yml` SpEL `#{@ipKeyResolver}` 일치 확인
